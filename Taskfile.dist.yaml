version: '3'

tasks:
  lint:
    cmds:
      - golangci-lint run ./...

  test:
    cmds:
      - docker compose -f docker-compose.dist.yaml run --rm --build test
      - docker compose -f docker-compose.dist.yaml down
